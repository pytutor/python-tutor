% Einfache Berechnungen
import numpy as np.

a = 3.2
b = 2.1

sum3     = a + b
diff3 = a - b
prod3   = a * b
quot3  = a / b
pot3    = a^b

wurzel3    = np.sqrt(a)
root3c     = a**(1/3)
expo3      = exp(-a)

trig1c     = np.sin(a)
trig2c     = np.sin(a+b)
trig3c     = np.cos(pi*a)

   
